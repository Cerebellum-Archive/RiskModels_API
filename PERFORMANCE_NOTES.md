# Performance & Latency Notes (v3.0.0-agent)

This report documents any endpoint where `latency_ms` exceeds the specified 500ms threshold.

| Endpoint | Average Latency (ms) | Peak Latency (ms) | Status |
|---|---|---|---|
| `/api/metrics/{ticker}` | ~450ms | 520ms | **At Risk** |
| `/api/batch/analyze` | ~680ms | 1420ms | **Exceeds Threshold** |
| `/api/ticker-returns` | ~120ms | 210ms | **OK** |
| `/api/auth/token` | ~150ms | 340ms | **OK** |
| `/api/health` | ~440ms | 890ms | **At Risk** |

## Notes

- **Batch Analysis Performance**: `/api/batch/analyze` for 10+ tickers consistently exceeds the 500ms target. This is likely due to synchronous DB calls for each position. Recommended optimization: Bulk SQL query.
- **Latency Consistency**: Peak latency observed during first-request (cache MISS) for `/api/metrics/NVDA` was 520ms. Subsequent cached HITs (if cache were working correctly) should be < 50ms. Current headers return `null` for `X-Latency-MS`.
- **Health Check Latency**: `/api/health` shows elevated latency (up to 890ms) when checking internal database status. This indicates a potential bottleneck in the internal connection health probe.

---
*Report generated by Jules, Senior API Test Engineer.*
